const home = document.getElementById("home");
const cursor = document.querySelector("cursor");

home.addEventListener("mouseover", () => {
  home.style.background = "rgb(24,24,24)";
  home.style.cursor = "pointer";
});
